// @ts-check
import { defineConfig } from 'astro/config';

// L'indirizzo definitivo lo aggiorneremo quando il sito sarà online.
// Per ora va bene così: non blocca nulla.
export default defineConfig({
  site: 'https://sitoportfolio.pages.dev',
});
